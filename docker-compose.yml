services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gainslab
    ports:
      - "5432:5432"   # host:container
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: